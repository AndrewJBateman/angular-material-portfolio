/**
 * Area model class.
 * Defines the data model for an area section on the page.
 * Contains id, title, subtitle, svgPath and content fields.
 */
export class Area {
	id: number;
	title: string;
	subtitle: string;
	svgPath: string;
	content: string;
}
