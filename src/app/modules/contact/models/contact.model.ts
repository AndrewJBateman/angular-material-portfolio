/**
 * ContactModel defines the shape of the contact form data model.
 * It contains the user's name, email and message fields.
 */
export interface ContactModel {
	name: string;
	email: string;
	message: string;
}
