export class Project {
  title: string;
  summary: string;
  icon: string;
  images: [
    { src: string, page: string, content: string, icon: string },
    { src: string, page: string, content: string, icon: string },
    { src: string, page: string, content: string, icon: string},
    { src: string, page: string, content: string, icon: string }
  ];
	github: string;
	url: string;
}
